const list = ['молоко', 'пиво', 'пиво', 'молоко', 'молоко'];
for (let i = 0; i < list.length; i++) {
    if (list[i] === 'молоко') {
        console.log('хорошо');
    }
    else {
        console.log('плохо');
    }
}